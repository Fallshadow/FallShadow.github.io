# 021_LightEffect

```Cpp
float3 L_Point_Dir = _Light021PointPos - pixel.worldPos;
float3 L_Point = normalize(L_Point_Dir);
float distance = length(L_Point_Dir);
float linearFade = 1 - saturate(distance _Light021PointColorAndRange.w);
linearFade = pow(linearFade,_LightPointLinearFadeSoft);
float4 wrapLambert_P = Diffuse(N, L_Point,_WrapLight);
float4 speculary_P = Speculary(N, L_Point, V,_VHMult,_BlinPhong_Exp, _BlinPhong_Scale);
float4 blinnPhong_P = BlinnPhong(N, L_Point, V,_WrapLight, _VHMult,_BlinPhong_Exp,_BlinPhong_Scale);
if (_LightType == 0)
{
    // return frac(_Light021PointColorAndRange.xyzz * _Light021PointColorAndRange.w * _Light021PointPos.xyzz);
    return _Light021PointColorAndRange.xyzz * linearFade * baseMap * wrapLambert_P * 10;
}
else if (_LightType == 1)
{
    // 距离平方衰减！
    float atten = 1.0 / max(distance * distance, 0.01);
    
    return _Light021PointColorAndRange.xyzz * atten * linearFade * baseMap * blinnPhong_P;
}
```

# 022_VirtualLight

特制 shader，营造出一盏灯的照射效果，只影响此 shader 材质本身。

## 用途

- 人物进入山洞，要额外提高人物的亮度
- 做一些点光面积光的效果模拟

## 实现

使用局部坐标计算距离，使用 exp 函数，表达距离近的有显示，距离远的无显示，加上颜色。

## 进阶

相当于自己创建一个灯光了，只影响这些 shader 下的。

# 023_NormalMap


# 024_视差贴图

其实就是把 V 和普通 UV 交点连线，然后延长一定深度得到新的 UV，再进行采样。

float4 ParallaxMappingOutUV(sampler2D ParallaxMap, float depth , float2 uv,float4 ParallaxMap_ST, float3 viewTS,out float2 ParallaxUV)
{
    uv = uv*ParallaxMap_ST.xy + ParallaxMap_ST.zw;
    
    float cosTheta = dot(viewTS,float3(0,0,1));
    float viewTSLength = depth / cosTheta;
    float3 startPoint = float3(uv,0);
    float3 endPoint = startPoint + viewTS*viewTSLength;
    float4 parallax = tex2D(ParallaxMap,saturate( endPoint.xy));
    ParallaxUV= endPoint.xy;
    return parallax;
}

对于背面，QUAD 正面 左边零，那背面其实就是 右边零，所以要 X 取反

视差还有一个简单的公式用的时候自己弄吧

步进视差，可以实现有厚度的效果

视差可以实现高度图！

# 025_CubeMapAndMatcup

CubeMap 可以实现反射，配合菲涅尔可以实现被打湿的感觉，配合 mipmap 可以模拟光滑的反射或者毛玻璃反射效果。

Matcup 就是混合显示达到效果 注意贴图要四周有变化 不然的话应用起来就会有问题 四周纯色 那就把

# 026_翅膀蝴蝶

主要是煽动翅膀，需要把翅膀和身体分离，有好几种思路
- mask : 直接增加一张黑白区分翅膀和身体的贴图
- uv : 增加 uv 通道、uv 上下拆分 以 0.5 判断、或者 0 - 1 1 - 2

## 视差彩色流动星星

- 使用 view dir noise 做视差星星
- 使用 hsv 做彩色
- 使用 time noise 做流动

## 流动雾

随便弄个噪音 UV 制造云雾效果

## 扇动翅膀

直接使用方法，有几个需要注意的是，旋转轴，枢轴点，左右翅膀不同角度的旋转，只控制翅膀的 mask 。

## 移动之后翅膀震动

将世界坐标加到时间扇动翅膀那里去，制作移动。

# 027_瓶装液体

发现模型有内外两层都有面片，影响显示，还发现UV里上面就是里层的，那就可以通过UV判断Y大于多少的就丢弃掉了。

- 基础 matcup 
- 用菲涅尔扰动（用噪音也行）做个 matcup 折射（其实原本应该用）
- cubemap 反射
- 使用菲涅尔混合反射和基础颜色
- 使用噪音模拟灰尘强度
- 使用基础色和折射选择最大值，得到灰度图，作为 alpha 图（这个 alpha 结合了前面说的不要渲染模型多余的内侧）
- 翻转法线渲染内侧（注意内外侧的显示次序，也就是层级3000）