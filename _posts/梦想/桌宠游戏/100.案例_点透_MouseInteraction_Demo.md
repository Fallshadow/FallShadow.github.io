- [关卡蓝图](#关卡蓝图)
  - [初步设置](#初步设置)
    - [1. 拿到设置](#1-拿到设置)
    - [2. 改变窗口显示模式](#2-改变窗口显示模式)
    - [3. 立即应用窗口更改](#3-立即应用窗口更改)
    - [4. 拿到玩家控制，显示鼠标](#4-拿到玩家控制显示鼠标)
    - [5. CMD 关闭抗锯齿、150 缩放、锁 60 帧](#5-cmd-关闭抗锯齿150-缩放锁-60-帧)
  - [ConfigureAdvancedWindowFeatures](#configureadvancedwindowfeatures)
  - [Set View Target with Blend](#set-view-target-with-blend)
  - [UI](#ui)
  - [射线事件](#射线事件)
- [场景](#场景)
  - [PostProcessVolume](#postprocessvolume)
    - [Post Process Materials](#post-process-materials)
    - [Global Illumination](#global-illumination)


# 关卡蓝图

## 初步设置

### 1. 拿到设置

Get Game User Settings 是一个全局的蓝图函数，专门用来在运行时读取和修改玩家的游戏设置。

返回一个 UGameUserSettings 类型的对象。这个对象是全局唯一的，里面存着当前玩家所有的游戏选项，比如分辨率、全屏模式、帧率上限、图形质量、音量等等。

### 2. 改变窗口显示模式

Set Fullscreen Mode 是 UGameUserSettings 提供的一个函数，专门用来强制改变游戏窗口的显示模式

- Fullscreen	传统的全屏独占模式
- Windowed Fullscreen	无边框窗口化全屏
- Windowed	标准窗口模式

### 3. 立即应用窗口更改

Apply Resolution Settings 是 UGameUserSettings 里的一个函数，它的作用是把所有待处理的分辨率和全屏模式修改真正地、立即地应用到游戏窗口上

Set Fullscreen Mode 这类函数，只是把你想要的结果先记录在内存里。只有当你调用了 Apply Resolution Settings，引擎才会去执行底层的窗口创建或调整，把所有记录的设置一次性兑现。

- Apply Resolution Settings	轻量级。只应用屏幕分辨率、全屏模式、窗口位置等与画面输出相关的设置。
- Apply Settings	重量级。应用所有设置，包括画质、帧率、分辨率、阴影质量等等，范围广得多。

### 4. 拿到玩家控制，显示鼠标

Get Player Controller：获取当前关卡里控制玩家的那个“大脑”。

Set Show Mouse Cursor：这个函数是 Player Controller 的成员函数，专门用来控制鼠标指针的显示与隐藏。

### 5. CMD 关闭抗锯齿、150 缩放、锁 60 帧

Execute Console Command 是一个蓝图函数，作用是在蓝图里执行你用键盘在控制台（~键）里输入的任意命令。

r.AntiAliasingMethod 0 ：关闭抗锯齿
r.ScreenPercentage 150 ：以 150% 分辨率渲染，然后再缩回 100% 保证渲染正确
t.maxfps 60 ：锁 60

## ConfigureAdvancedWindowFeatures

通过插件的 BPL 设置窗口的样式：透明、无边框、穿透、置顶

## Set View Target with Blend

平滑地将视角从一个目标切换到另一个目标，这里目的是将摄像机固定在目标视角

## UI

Create Widget & Add to Viewport

## 射线事件

Set Hit-Test Type : Hit Type 只有 None 和 Game Raycast

Set Game Raycast Trace Channel

Set Hit-Test Enabled

通过玩家控制器为场景中的物体施加点击事件

# 场景

## PostProcessVolume

### Post Process Materials

后处理材质运行在屏幕空间，它拿到的输入是整个场景已经渲染好的颜色画面。确保 PostProcessVolume 覆盖整个场景

这里使用的是插件的材质 M_PP_DistanceMask

主要使用的是 SceneColor 节点，读取不同的 texture

- CustomDepth ：读取屏幕上每个像素的自定义深度值。这个值只由那些被设置为“渲染自定义深度”的物体写入。CustomDepth 缓冲区是一个专门记录深度的画面，但它不是哪里都有值的。
  - 有值的区域：只有那些开启了 “渲染自定义深度” 的物体，才会把自己的深度写进去。
  - 无值的区域：对于场景中其他所有物体，包括天空、背景、地面以及你用来当背景的桌面，只要没开这个选项，它们就不会写入任何数据。
  - 当一个像素没被写入任何数据时，CustomDepth 缓冲区会为它填入一个预设的极值，这个值就代表了无穷远。
- PostProcessInput0 ：首先，场景渲染完成，产出 PostProcessInput0（场景颜色） 经过这个材质处理之后 就会变成 PostProcessInput1

游戏输出的时候，没有东西的地方就是黑色，那个地方的像素点深度是无限远。
示例场景的后处理中使用了 后处理材质，材质逻辑是：深度大于 10000 的像素点强行透明遮罩，所以原来黑色的地方 alpha 的值 就是 0，而进处的物体设置为不透明 alpha 就是 1，这样再配合窗口的透明就可以达成真正的透明

### Global Illumination

全局光照设置为 屏幕空间，减少性能开销，增加接触面阴影，对于桌宠来说很合适
